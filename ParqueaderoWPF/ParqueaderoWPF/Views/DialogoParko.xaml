<Window x:Class="DialogoParko"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
        FontFamily="Segoe UI">
    <Border CornerRadius="14" Background="White" MinWidth="400" MaxWidth="500" Margin="14">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.35" BlurRadius="22" ShadowDepth="3"/>
        </Border.Effect>
        <StackPanel>
            <!-- Encabezado PARKO -->
            <Border Background="{StaticResource BrushLateral}" CornerRadius="14,14,0,0" Padding="16,11">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Assets/logo.png" Width="24" Height="24"/>
                    <TextBlock x:Name="lblTitulo" Text="PARKO" Foreground="White" FontSize="14"
                               FontWeight="Bold" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Mensaje -->
            <StackPanel Orientation="Horizontal" Margin="22,20,22,4">
                <TextBlock x:Name="lblIcono" Text="ℹ️" FontSize="34" VerticalAlignment="Center"/>
                <TextBlock x:Name="lblMensaje" FontSize="13.5" TextWrapping="Wrap" MaxWidth="360"
                           Margin="16,0,0,0" VerticalAlignment="Center"
                           Foreground="{StaticResource BrushTexto}"/>
            </StackPanel>

            <!-- Botones -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="22,14,22,18">
                <Button x:Name="btnNo" Content="No" Style="{StaticResource BotonGris}"
                        Width="100" Height="36" Margin="0,0,10,0" Visibility="Collapsed" IsCancel="True"/>
                <Button x:Name="btnSi" Content="Aceptar" Style="{StaticResource BotonExito}"
                        Width="110" Height="36" IsDefault="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
